# CLAUDE.md — Zenn の記事置き場

**Zenn の GitHub 連携で記事を管理するリポジトリ。** `articles/` の `.md` がそのまま記事になる。

| 知りたいこと | 見る場所 |
| --- | --- |
| Zenn の書式 | https://zenn.dev/zenn/articles/markdown-guide |
| 記事のもとになった実装 | `../jev-16types`（poina）／ `../myhp` |
| 開発の作法 | `../knowledge/` |

## 手順

```bash
npx zenn preview                 # http://localhost:8000 で確認
npx zenn new:article --slug xxx  # 新しい記事
```

- **slug は `a-z0-9_-` の 12〜50 字。** あとから変えると URL が変わる
- 画像は `images/` に置いて `/images/xxx.png` で参照する

## ★ 公開は本人がやる ★

**`published: false` のまま push してよい。** Zenn 側では下書きになる。

★ **`published: true` に変えるのは本人。** ★ こちらから勝手に公開しない。
**push も本人**（`../knowledge/02-workflow.md` と同じ）。

---

## ★ 記事に書いてはいけないこと ★

**poina の記事を書くときは、サイト側と同じ縛りが効く**（`../jev-16types/docs/legal-check.md`）。

- ★ **タイトル・見出しに `MBTI` を入れない** ★ —— 日本の登録商標（第5292740号、**第41類を含む**）。
  本文で**打ち消し・事実の記述として触れる**のは可。いまの下書きは**本文に1箇所だけ**
- ★ **「16タイプ性格診断」「16タイプ◯◯診断」の形を、サービス名として書かない** ★ ——
  「16タイプ＋用途」は既存の登録商標に近づく。**「16タイプ」は説明語としてだけ使う**
- **他サービスのタイプ愛称を書かない** —— 建築家 / 仲介者 / 分析家 など。**翻訳成果物**
- ★ **poina と myhp のリポジトリへのリンクを置かない** ★ —— **どちらも private**。
  貼ると**読者全員が 404 を見る**
- **Cloudflare のアカウントIDを載せない** —— ★ **コード片だけでなくスクリーンショットも見る** ★
- **権利の話を書くときは「これは法的な助言ではない」を明記する**

## 書き方

★ **`docs/writing-style.md` を先に読む**（本人からもらった指摘の記録。AI が書いた感じを抜く／届くところに置く）。★
`../knowledge/04-words.md` にも寄せる。**`04` の「声の高さ」は、ここの 0〜4 を画面の文言にも効かせたもの**（2026-09-22）。

- **「さいきん」「いま」「〜中」を書かない** —— 記事は残る。**放置した瞬間に嘘になる**
- **数字は実測値を書く。** 「速い」ではなく「290〜320ms」
- ★ **踏んだ穴は隠さない。** ★ 記事の価値はだいたいそこにある

## コミット規約

```
種別(範囲): 内容

本文（なぜそう書いたかを書く）
```

- **種別** — `feat`（新しい記事） / `fix` / `docs` / `chore`
- **範囲** — 記事の slug（例: `jev`）
- 日本語で書く

コミット者情報は ★ **`--global` ではなくこのリポジトリだけ** ★ に入れてある
（`petitbouquet` / noreply）。`git config --global` は**触らないこと**。
